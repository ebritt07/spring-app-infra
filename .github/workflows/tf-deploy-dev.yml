name: tf-deploy (dev)

permissions:
  id-token: write
  contents: read

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      apply-changes:
        description: 'Apply terraform changes (DEV) ?'
        required: true
        default: 'no'

jobs:
  tf-plan:
    uses: ./.github/workflows/tf-plan.yml
    with:
      env: "dev"
      role_to_assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}

  tf-apply:
    needs: tf-plan
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.apply-changes == 'yes' }}
    uses: ./.github/workflows/tf-apply.yml
    with:
      env: "dev"
      role_to_assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}